query checkSeason9Conditions {
  season9Conditions(
    where: {
      hasAllRequiredTokens: true
    }
    first: 1000,
    orderBy: lastUpdated,
    orderDirection: desc
  ) {
    id  # This is the address
    hasAllRequiredTokens
    lastUpdated
  }
}