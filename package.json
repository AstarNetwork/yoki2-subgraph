{
  "name": "yoki2",
  "license": "UNLICENSED",
"scripts": {
  "codegen": "graph codegen",
  "build": "graph build",
  "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ yoki2",
  "create-local": "graph create --node http://localhost:8020/ yoki2",
  "remove-local": "graph remove --node http://localhost:8020/ yoki2",
  "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 yoki2 --version-label v0.0.1",
  "test": "graph test"
},
  "dependencies": {
    "@graphprotocol/graph-cli": "0.59.0",
    "@graphprotocol/graph-ts": "0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
